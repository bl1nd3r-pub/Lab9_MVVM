<UserControl x:Class="Lab11_Navigation.Views.ContactEditView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Lab11_Navigation.Views"
        mc:Ignorable="d">
    <StackPanel Margin="20" HorizontalAlignment="Center" VerticalAlignment="Top">
        <TextBlock Text="Редактирование контакта" FontSize="20" FontWeight="Bold" Margin="0,0,0,15"/>

        <TextBlock Text="Имя:" Margin="0,0,0,5"/>
        <TextBox Text="{Binding EditName, UpdateSourceTrigger=PropertyChanged}" Width="200" Margin="0,0,0,10"/>

        <TextBlock Text="Телефон:" Margin="0,0,0,5"/>
        <TextBox Text="{Binding EditPhone, UpdateSourceTrigger=PropertyChanged}" Width="200" Margin="0,0,0,15"/>

        <StackPanel Orientation="Horizontal">
            <Button Content="Сохранить" Command="{Binding SaveCommand}" Width="100" Margin="0,0,10,0"/>
            <Button Content="Отмена" Command="{Binding CancelCommand}" Width="100"/>
        </StackPanel>
    </StackPanel>
</UserControl>
